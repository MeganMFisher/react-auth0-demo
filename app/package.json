{
  "name": "app",
  "version": "0.1.0",
  "private": true,
  "proxy": "http://localhost:4005", //backend port
  "dependencies": {
    "react": "^15.6.1",
    "react-dom": "^15.6.1"
  },
  "devDependencies": {
    "react-scripts": "1.0.10"
  },
  "scripts": {
    "start": "PORT=4000 react-scripts start", //will always start frontend on defined port. 
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}